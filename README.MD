## Create Docker container with Mysql and PHPMyAdmin for dev environment

* Edit **.env** file with your settings

* Run 
```
docker-compose up -d
```

* Mysql data files will be in **/docker/mysql/data** folder

